class Snowflake{

    /*constructor(){
        var xAxis = float.random(0, width); 
        var yAxis = float.random(-25, -5);
        var mass = float.random(2, 15); // this is gonna determine how big the snowflake is
        //and which will also determine how fast it will fall
        var speed = 15 + mass;
        var direction = random([-0.3, 0.3]); // snowflakes will either fall right way or left way slanted
    
    }*/

    fall(){
        //ellipse(xAxis + direction, yAxis - speed, mass, mass);
        ellipse(50, 50, 50, 50);
    }
  
  
  
}
